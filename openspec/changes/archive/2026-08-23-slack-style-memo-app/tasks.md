## 0. 派生した未決事項の確定

- [x] 0.1 PostgreSQL の運用形態を確定する（自前コンテナ / RDS）。**RDS を選ぶ場合は `pg_bigm` の利用可否を必ず確認する（Aurora PostgreSQL では提供されていない）** → D18: 自前コンテナ
- [x] 0.2 コンテナを動かすホスト（EC2 / VPS / 他）とオーケストレーション手段、独自ドメインと DNS の管理主体を確定する → D19: 単一 VPS + Compose、ドメインは `memo.sudabon.com`
- [x] 0.3 Cognito の認証 UI（Hosted UI / カスタム UI）と、Electron・PWA・iOS それぞれのリダイレクト URI 設計を確定する → D20: Hosted UI + PKCE
- [x] 0.4 AWS リソースの構築手段（IaC / 手動）を確定する → D21: 手動（コンソール / CLI）
- [x] 0.5 添付の上限値（1 ファイルの最大サイズ・1 ポストの最大添付数・許可 MIME type）を確定する → D22
- [x] 0.6 論理削除されたポストの表示方針（プレースホルダを残す / 完全に隠す）と、返信を持つポストが削除された場合のスレッドの見え方を確定する → D23
- [x] 0.7 スレッド返信をチャネルのタイムラインにも出す機能（Slack の「チャネルにも投稿する」相当）の要否を確定する → D24: 不要
- [x] 0.8 絵文字カタログの登録タイミングと、Unicode 標準絵文字の併用可否、png の配信経路を確定する → D25
- [x] 0.9 タイムゾーンとロケールの方針（日付セパレータの判定基準、UI 文言の言語）を確定する → D26

## 1. M0 — 基盤と前提検証

- [x] 1.1 モノレポのディレクトリ構成（`api/` `backend/` `frontend/` `electron/` `mobile/` `emoji/` `infra/`）を作成し、README に構成を記す
- [x] 1.2 **`pg_bigm` を含む PostgreSQL のコンテナイメージを用意し、`create extension pg_bigm` が通ることを確認する**（D12 の前提。ここが崩れると全文検索の方式を再検討する）
- [x] 1.3 `backend/` に Go モジュールを初期化し、ヘルスチェック用エンドポイントを返すところまで動かす
- [x] 1.4 `frontend/` に Vite + React + TypeScript プロジェクトを作成する
- [x] 1.5 `frontend/` に TailwindCSS と shadcn/ui を導入し、サンプルコンポーネントが描画されることを確認する
- [x] 1.6 `electron/` に Electron のメインプロセスとパッケージング設定を作り、`frontend/` のビルド成果物を同梱して起動できることを確認する
- [x] 1.7 `mobile/` に Flutter プロジェクトを作成し、iOS シミュレータで起動することを確認する
- [x] 1.8 `infra/` に Nginx / Certbot / API / PostgreSQL の 4 コンテナを起動する compose 定義を作る
- [x] 1.9 Nginx の設定を書く（TLS 終端、API へのリバースプロキシ、PWA 静的アセット配信、SPA フォールバック、HTTP → HTTPS リダイレクト）
- [x] 1.10 **Nginx の SSE 用設定（`proxy_buffering off` と十分な `proxy_read_timeout`）を入れる**
- [x] 1.11 HTTP → HTTPS リダイレクトから `/.well-known/acme-challenge/` を除外する
- [ ] 1.12 Certbot による証明書の初回取得を通し、HTTPS で応答することを確認する → **M9（10.1）へ延期**。開発中は自己署名証明書
- [ ] 1.13 証明書更新後に Nginx をリロードする deploy hook を組み込み、更新が反映されることを確認する → **M9（10.2）へ延期**
- [x] 1.14 PostgreSQL のデータと証明書を、コンテナ再作成をまたいで残る永続ボリュームに配置する
- [x] 1.15 API サーバと PostgreSQL がインターネットへ直接公開されていないことを確認する
- [x] 1.16 各コンテナのヘルスチェックを定義する（API は PostgreSQL への接続可否を含めて返す）
- [x] 1.17 Cognito ユーザープールと、添付用の S3 バケット（パブリック読み取り禁止）を用意する
- [x] 1.18 CI を用意する（Go の build/vet/test、TypeScript の型検査/lint/test、Flutter の analyze/test）
- [x] 1.19 CI に「OpenAPI 生成物が最新か」を検査するジョブを追加する（生成 → `git diff --exit-code`）
- [x] 1.20 UI コンポーネントから Electron 固有 API を直接参照することを禁止する lint ルールを追加する

## 2. M1 — 認証とチャネル階層

- [x] 2.1 `api/openapi.yaml` を作成し、エラー応答の共通形式とチャネル関連エンドポイントを定義する
- [x] 2.2 OpenAPI から Go サーバ型を生成する仕組み（oapi-codegen）を組み込み、生成コマンドを Makefile 等に登録する
- [x] 2.3 マイグレーション基盤を導入し、バイナリ埋め込みで起動時に未適用分を検出できるようにする
- [x] 2.4 `users` テーブルのマイグレーションを作成する（Cognito の識別子を保持）
- [x] 2.5 Cognito の JWKS を取得・キャッシュし、署名・発行者・オーディエンス・有効期限をローカル検証する認可ミドルウェアを実装する
- [x] 2.6 鍵ローテーション時に公開鍵を取得し直す処理を実装する
- [x] 2.7 初回サインイン時にユーザーを登録し、2 回目以降は既存ユーザーを再利用する処理を実装する
- [x] 2.8 `channels` テーブルのマイグレーションを作成する（id / parent_id / name / sort_key / 日時）
- [x] 2.9 **同一階層での名前の一意性を部分ユニーク索引で保証する（`parent_id is not null` 用と `parent_id is null` 用の 2 本）**
- [x] 2.10 fractional index（兄弟間の順序キー）の生成・比較ロジックを実装し、境界ケースとキー長の上限処理の単体テストを書く
- [x] 2.11 `internal/store` にチャネルのリポジトリを実装する（pgx + sqlc、階層取得は `WITH RECURSIVE`）
- [x] 2.12 チャネルの作成・リネームを実装し、名前の衝突をサーバ側で拒否する
- [x] 2.13 チャネル移動（親の付け替え＋並び替え）を実装し、適用後の親と `sort_key` を応答で返す
- [x] 2.14 循環（自身の子孫を親にする移動）を再帰 CTE で検出して拒否する
- [x] 2.15 移動先での名前衝突を検出して拒否する
- [x] 2.16 **ポストを持つチャネル（子孫も含む）の削除を拒否する処理を実装する。論理削除済みポストのみの場合は削除可能とする**
- [x] 2.17 チャネルのハンドラを実装し、一貫したエラー形式で応答する
- [x] 2.18 testcontainers による PostgreSQL 統合テストで `authentication` と `channel-hierarchy` の各 Scenario を検証する

## 3. M2 — ポストとスレッド

- [x] 3.1 `posts` テーブルのマイグレーションを作成する（channel_id / thread_root_id / author_id / body / 日時 / edited_at / deleted_at と部分索引）
- [x] 3.2 `api/openapi.yaml` にポストとスレッドのエンドポイントを追加し、生成物を更新する
- [x] 3.3 ポストの作成を実装する（本文と添付がともに空の場合は拒否する）
- [x] 3.4 `(created_at, id)` を用いた keyset pagination でポスト一覧を取得する API を実装する（昇順応答＋続きのカーソル）
- [x] 3.5 **チャネルを開いた直後に最新 10 件のみを取得する経路を実装する**
- [x] 3.6 タイムラインの取得が `thread_root_id is null` の行のみを返すことを保証する
- [x] 3.7 ポストの編集を実装する（本文は上書き、`edited_at` を記録、履歴は保持しない）
- [x] 3.8 **ポストの論理削除を実装し、全取得クエリが削除済みを除外することを保証する**
- [x] 3.9 削除済みポストへの編集要求を拒否する
- [x] 3.10 スレッド返信の作成を実装する（`thread_root_id` を親から継承し、多段スレッドを作らない）
- [x] 3.11 スレッド内の返信一覧を古い順で取得する API を実装する
- [x] 3.12 親ポストの返信件数と最終返信日時を返す処理を実装する
- [x] 3.13 0.6 の決定に従い、論理削除されたポストの応答上の扱い（プレースホルダ / 完全除外）を実装する
- [x] 3.14 `post-timeline` と `post-threads` の各 Scenario を統合テストで検証する

## 4. M3 — フロントエンド骨格（Electron）

- [x] 4.1 **プラットフォームアダプタのインターフェースを定義する**（メニュー、ショートカット、ファイル選択、ファイル保存、通知、資格情報保管、永続化、および各能力の利用可否）
- [x] 4.2 アダプタの Electron 実装を書く（IPC 経由）
- [x] 4.3 テスト用のフェイクアダプタを用意する
- [x] 4.4 OpenAPI から TypeScript 型を生成し、fetch ベースの API クライアントを実装する
- [x] 4.5 Cognito の認可コードフロー（PKCE）によるサインイン・サインアウトを実装する
- [x] 4.6 トークンをアダプタ経由で安全に保管し、期限前の自動更新と失敗時の再認証を実装する
- [x] 4.7 未サインイン時にサインイン画面を表示し、コンテンツを描画しないようにする
- [x] 4.8 TanStack Query と Zustand を導入し、サーバ状態と UI 状態の置き場を分ける
- [x] 4.9 3ペインレイアウト（左=チャネルツリー、右上=タイムライン、右下=入力フォーム）を実装する
- [x] 4.10 サイドバーのリサイズ・折りたたみを実装し、幅を永続化する
- [x] 4.11 チャネルツリーを実装する（階層表示、展開／折りたたみ、展開状態の永続化、深い階層でも名前が読める表示）
- [x] 4.12 チャネルの作成・リネーム・削除の UI を実装する（名前衝突とポスト存在による削除拒否の表示を含む）
- [x] 4.13 dnd-kit でチャネルの drag & drop を実装する（子として入れる／並べるの視覚的区別、子孫へのドロップ拒否）
- [x] 4.14 D&D の楽観的更新と、サーバ拒否時のツリー再取得による巻き戻しを実装する
- [x] 4.15 タイムラインを実装する（古い順表示、**初期 10 件**、日付セパレータ、上方向スクロールでの動的取得とスクロール位置維持、多重要求の抑止）
- [x] 4.16 入力フォームを実装する（最下部固定、複数行と自動拡張、Enter 送信／Shift+Enter 改行、送信中の多重送信防止、失敗時に入力を失わない）
- [x] 4.17 チャネルごとの下書き保持と、再起動をまたいだ復元を実装する
- [x] 4.18 ポストの編集・削除の UI（ホバーで現れる操作メニュー）を実装する
- [x] 4.19 スレッドパネルを実装する（タイムラインを保持したまま併置、親ポスト＋返信の古い順表示、返信専用フォームと独立した下書き）
- [x] 4.20 タイムライン上の返信件数と最終返信日時の表示、およびスレッドを開く導線を実装する
- [x] 4.21 ウィンドウサイズ・位置・選択チャネルの保存と復元を実装する
- [x] 4.22 キーボードショートカット（入力フォームへのフォーカス、新規チャネル、検索、チャネル間移動）を実装し、テキスト編集の標準ショートカットを妨げないことを確認する
- [x] 4.23 アプリケーションメニューと設定画面（接続先、テーマ、サインアウト）を実装する
- [x] 4.24 ライト／ダーク／システム追従のテーマ切り替えを実装する
- [x] 4.25 バックエンドへ接続できない場合のエラー表示と再試行導線を実装する
- [x] 4.26 Playwright で主要導線（サインイン → チャネル作成 → 投稿 → スレッド返信 → D&D で移動 → 再起動後の状態復元）の E2E テストを書く

## 5. M4 — PWA 配信

- [x] 5.1 Web App Manifest とアイコン一式を用意する
- [x] 5.2 アダプタのブラウザ実装を書く（ファイル選択、ダウンロード、通知、`localStorage` による永続化、能力の利用可否）
- [x] 5.3 ブラウザで利用できない機能（グローバルショートカット等）の導線を隠す処理を実装する
- [x] 5.4 Service Worker を実装し、アプリシェル（HTML / JS / CSS / アイコン）をキャッシュする
- [x] 5.5 **チャネル・ポスト・添付の内容を Service Worker でキャッシュしないことを確認する**
- [x] 5.6 新バージョン配信時の更新適用と、古いキャッシュの破棄を実装する
- [x] 5.7 オフラインで開いた際にアプリシェルを表示しつつ接続不可を明示する挙動を実装する
- [x] 5.8 PWA でのトークン保管方針（永続 web ストレージへ平文で保存しない）を実装する
- [x] 5.9 Nginx から PWA の静的アセットを配信し、インストール可能であることを実機ブラウザで確認する
- [x] 5.10 `pwa-delivery` の各 Scenario を検証する（フェイクアダプタでの分岐テストを含む）

## 6. M5 — リッチコンテンツと添付

- [x] 6.1 `react-markdown` + `remark-gfm` を導入し、基本記法（見出し・強調・リスト・引用・表・リンク）を描画する
- [x] 6.2 `rehype-sanitize` で危険な HTML・スクリプト・危険なスキームのリンクを無害化し、実行されないことをテストで確認する
- [x] 6.3 リンククリックを既定ブラウザで開くようにし、アプリ内遷移が起きないことを確認する
- [x] 6.4 Shiki を導入し、言語指定ありのコードブロックをハイライトする（未対応言語・言語指定なしはプレーン表示へフォールバック）
- [x] 6.5 コードブロックの横スクロールとコピー操作を実装する
- [x] 6.6 `mermaid` コードブロックを図として描画する。**投稿確定済みのポストのみを対象とし、入力中のライブプレビューは実装しない**
- [x] 6.7 Mermaid の描画失敗時にコードブロックへフォールバックし、他ポストの描画を継続する
- [x] 6.8 Mermaid の図／ソース表示切り替えと、可視範囲に入ってから描画する遅延描画を実装する
- [x] 6.9 コードハイライトと Mermaid の配色をテーマに追従させる
- [x] 6.10 入力フォームのコードブロック挿入補助と、未閉コードブロック内での Enter が送信にならない挙動を実装する
- [x] 6.11 `attachments` テーブルのマイグレーションを作成する（post_id / ファイル名 / 形式 / サイズ / 保存キー / チェックサム）
- [x] 6.12 `blob.Store` インターフェースと S3 実装を書く
- [x] 6.13 `api/openapi.yaml` に添付のアップロード開始・完了通知・ダウンロード URL 発行のエンドポイントを追加し、生成物を更新する
- [x] 6.14 アップロード用の署名付き URL 発行を実装する（0.5 で決めた上限と MIME type、および認可をサーバ側で検証）
- [x] 6.15 アップロード完了通知によるメタデータ確定を実装する
- [x] 6.16 ダウンロード用の短期署名付き URL 発行を実装する（認可を検証、URL をログに残さない）
- [x] 6.17 未確定の添付と、論理削除されたポストの添付を回収するジョブを実装する（参照されている実体は削除しない）
- [x] 6.18 入力フォームへの添付追加を実装する（ファイル選択、ドラッグ＆ドロップ、クリップボード貼り付け、いずれもアダプタ経由）
- [x] 6.19 送信前の添付プレビューと除去、アップロード進捗表示、失敗時の再試行を実装する
- [x] 6.20 クライアント側の上限検証（サイズ・件数）とエラー表示を実装する
- [x] 6.21 画像添付のインライン表示（サムネイル、複数画像のグリッド、拡大ビューア、取得失敗時のプレースホルダ）を実装する
- [x] 6.22 非画像添付のカード表示とダウンロードを実装する
- [x] 6.23 `rich-content-rendering` と `attachments` の各 Scenario を検証する

## 7. M6 — 絵文字リアクション

- [x] 7.1 `emojis` / `reactions` テーブルのマイグレーションを作成する（`reactions` の主キーで重複付与を防ぐ）
- [x] 7.2 `emoji/` ディレクトリを走査し、ファイル名からショートコードを導出してカタログへ登録する処理を実装する
- [x] 7.3 チェックサムによる差分検出（内容が変わった png の更新、同一内容の再登録抑止）を実装する
- [x] 7.4 png 以外のファイルと、ショートコードとして不正なファイル名の扱いを実装する（処理を中断せず記録する）
- [x] 7.5 0.8 の決定に従い、登録処理の実行タイミングと png の配信経路を実装する
- [x] 7.6 `api/openapi.yaml` に絵文字一覧とリアクションのエンドポイントを追加し、生成物を更新する
- [x] 7.7 リアクションの付与・解除 API を実装する（重複付与は既存状態を維持して成功として扱う）
- [x] 7.8 絵文字ピッカー UI を実装する（一覧、ショートコードによる絞り込み、未登録時の表示）
- [x] 7.9 ポスト下部のリアクション表示を実装する（絵文字と件数、自身の付与の強調、付与者の提示、件数 0 での消去、画像取得失敗時のショートコード表示）
- [x] 7.10 リアクションの楽観的更新と失敗時の巻き戻しを実装する
- [x] 7.11 スレッド内の返信にも同じリアクション機能を適用する
- [x] 7.12 `emoji-reactions` の各 Scenario を検証する

## 8. M7 — 全文検索と変更通知

- [x] 8.1 `pg_bigm` 拡張の有効化と `posts.body` への GIN 索引作成をマイグレーションに追加する
- [x] 8.2 `api/openapi.yaml` に検索エンドポイントを追加し、生成物を更新する
- [x] 8.3 検索 API を実装する（bigram 索引を用いた本文検索、論理削除の除外、認可の検証）
- [x] 8.4 チャネルと投稿日時の範囲による絞り込みを実装する
- [x] 8.5 スレッド返信も検索対象に含め、所属スレッドが分かる形で返す
- [x] 8.6 検索結果のページングを実装する
- [x] 8.7 検索 UI を実装する（最小 2 文字の制約、抜粋と一致箇所の強調、チャネル名と日時の表示、0 件時の表示）
- [x] 8.8 検索結果から元のポストへ移動し、そのポストが見える位置にタイムラインを表示する処理を実装する
- [x] 8.9 SSE エンドポイントを実装し、チャネル・ポスト・スレッド・リアクションの変更イベントを配信する
- [x] 8.10 イベントに単調増加する ID を付与し、`Last-Event-ID` による再送を実装する
- [x] 8.11 切断中の差分をまとめて取得するフォールバック API を実装する
- [x] 8.12 フロントエンドで SSE を購読し、表示中チャネルへの新規ポスト・階層変更・返信件数・リアクションを反映する
- [x] 8.13 **Nginx 越しに SSE が届くことを実機で確認する（`proxy_buffering off` の効果検証）**
- [x] 8.14 接続断時の明示、変更操作の抑止、下書きの保持、接続回復後の最新化を実装する
- [x] 8.15 `full-text-search` と `sync-and-storage` の各 Scenario を検証する

## 9. M8 — iOS アプリ（Flutter）

- [x] 9.1 OpenAPI から Dart の型とクライアントを生成する仕組みを組み込む
- [x] 9.2 Riverpod による状態管理の土台を作る
- [x] 9.3 Cognito によるサインイン・サインアウトと、Keychain へのトークン保管、自動更新を実装する
- [x] 9.4 チャネル一覧画面とタイムライン画面の 2 画面ナビゲーションを実装する
- [x] 9.5 横向き・大型端末での並列表示レイアウトを実装する
- [x] 9.6 チャネル階層の展開／折りたたみと、長押しドラッグによる階層・並び順の変更を実装する（子孫へのドロップ拒否、名前衝突の拒否）
- [x] 9.7 タイムラインを実装する（古い順、初期 10 件、日付セパレータ、上方向の動的取得）
- [x] 9.8 入力フォームを実装する（最下部固定、ソフトキーボード追従、セーフエリア対応、下書き保持）
- [x] 9.9 ポストの編集・論理削除（長押しで操作メニュー）を実装する
- [x] 9.10 スレッド画面を実装する（独立画面、返信の投稿、戻る操作）
- [x] 9.11 リアクションの付与・解除・表示を実装する
- [x] 9.12 Markdown 描画とコードブロックのシンタックスハイライトを実装する（コードブロック内の横スクロール）
- [x] 9.13 WebView に mermaid.js を載せて Mermaid 図を描画し、可視範囲に入ったポストのみ描画・同時 WebView 数に上限を設ける
- [x] 9.14 写真ライブラリ・カメラ・ファイルからの添付と、権限拒否時の案内を実装する
- [x] 9.15 添付の表示（画像インライン、ファイルカード、ダウンロード）を実装する
- [x] 9.16 全文検索の画面を実装する
- [x] 9.17 SSE の購読と、バックグラウンド復帰時の差分取得フォールバックを実装する
- [x] 9.18 起動時に接続できない場合の表示（古いデータを最新として見せない）を実装する
- [x] 9.19 `mobile-shell` の各 Scenario を widget test / `integration_test` で検証する

## 10. M9 — デプロイと配布

- [x] 10.1 本番ホストへ 4 コンテナ構成を配備し、HTTPS で API と PWA が応答することを確認する
- [x] 10.2 **証明書の自動更新と、更新後の Nginx リロードが機能することを検証する**（更新失敗時に検知できる記録・通知手段を含む）
- [x] 10.3 コンテナ再作成後もデータと証明書が保持されることを確認する
- [x] 10.4 マイグレーション未適用の状態で起動しないことを確認する
- [x] 10.5 macOS アプリの署名・公証を設定し、配布物を作成する
- [x] 10.6 iOS アプリのビルド設定（バンドル ID、アイコン、対象 OS バージョン）を整える
- [x] 10.7 セットアップと運用手順（デプロイ、証明書、バックアップ、絵文字の登録）を README に記載する
- [x] 10.8 `deployment` の各 Scenario を検証する
- [x] 10.9 全 spec の Scenario がいずれかのテストに対応していることを確認し、抜けを埋める
- [x] 10.10 `openspec validate slack-style-memo-app` を実行し、実装と spec の齟齬を解消する
