# authentication Specification

## Purpose
TBD - created by archiving change slack-style-memo-app. Update Purpose after archive.
## Requirements
### Requirement: Cognito によるサインイン
システムは、マネージド認証サービスのユーザーディレクトリを用いてユーザーを認証しなければならない (SHALL)。未認証の状態では、チャネル・ポスト・添付のいずれにもアクセスできてはならない (MUST NOT)。

#### Scenario: 初回起動時にサインインを求める
- **WHEN** 認証情報を持たないユーザーがアプリを起動する
- **THEN** システムはサインイン画面を表示し、チャネルやポストの内容を表示しない

#### Scenario: サインインに成功する
- **WHEN** ユーザーが正しい認証情報でサインインする
- **THEN** システムはトークンを取得して保持し、チャネルツリーとタイムラインを表示する

#### Scenario: サインインに失敗する
- **WHEN** ユーザーが誤った認証情報でサインインを試みる
- **THEN** システムは失敗した旨を表示し、認証情報の詳細を伴わないエラーメッセージを示す

#### Scenario: サインアウトする
- **WHEN** ユーザーがサインアウト操作を実行する
- **THEN** システムは保持しているトークンを破棄し、キャッシュ済みのチャネル・ポストを画面から取り除いてサインイン画面へ戻る

### Requirement: 全クライアントでの認証
システムは、Electron デスクトップアプリ、PWA、iOS アプリのいずれにおいても同一のユーザーディレクトリで認証を行わなければならない (SHALL)。ブラウザ以外のクライアントは、アプリ固有の URL スキームによるコールバックで認証を完了できなければならない (SHALL)。

#### Scenario: 別クライアントで同じアカウントを使う
- **WHEN** ユーザーがデスクトップでサインインしているアカウントと同じアカウントで iOS アプリにサインインする
- **THEN** システムは同一のユーザーとして扱い、同じチャネルとポストを表示する

#### Scenario: ブラウザ以外でコールバックを受け取る
- **WHEN** Electron または iOS アプリでの認証が完了する
- **THEN** システムはアプリ固有の URL スキームでコールバックを受け取り、認証を完了させる

#### Scenario: 許可されていないコールバック先
- **WHEN** 認証サービスに登録されていないコールバック先が要求される
- **THEN** 認証サービスは要求を拒否し、トークンを発行しない

### Requirement: トークンの安全な保管
システムは、認証トークンをプラットフォームごとの安全な保管領域に格納しなければならない (SHALL)。ブラウザで動作する場合、トークンを永続的な web ストレージへ平文で保存してはならない (MUST NOT)。

#### Scenario: デスクトップでの保管
- **WHEN** Electron アプリがトークンを保持する
- **THEN** システムは OS の安全な資格情報保管領域を用いて保存する

#### Scenario: ブラウザでの保管
- **WHEN** PWA がトークンを保持する
- **THEN** システムはアクセストークンを永続 web ストレージへ平文で保存しない

#### Scenario: iOS での保管
- **WHEN** iOS アプリがトークンを保持する
- **THEN** システムは Keychain を用いて保存する

### Requirement: API 要求への認可付与
システムは、すべての API 要求に対して認可情報を付与しなければならない (SHALL)。サーバは認可情報を検証し、無効な要求を拒否しなければならない (SHALL)。

#### Scenario: 認可情報のない要求
- **WHEN** 認可情報を伴わない要求が API へ到達する
- **THEN** サーバは要求を拒否し、認可エラーを返す

#### Scenario: 署名が不正なトークン
- **WHEN** 署名の検証に失敗するトークンを伴う要求が API へ到達する
- **THEN** サーバは要求を拒否し、処理を行わない

#### Scenario: 発行者やオーディエンスが一致しないトークン
- **WHEN** 想定するユーザーディレクトリ以外が発行したトークン、または想定するオーディエンス向けでないトークンを伴う要求が API へ到達する
- **THEN** サーバは要求を拒否する

#### Scenario: 想定しない署名方式のトークン
- **WHEN** 認証サービスが用いる署名方式と異なる方式で署名されたトークンを伴う要求が API へ到達する
- **THEN** サーバは要求を拒否し、鍵の取得を試みない

#### Scenario: 検証は外部への問い合わせを伴わない
- **WHEN** サーバが API 要求のトークンを検証する
- **THEN** サーバはキャッシュ済みの公開鍵で検証し、要求ごとに認証サービスへ問い合わせない

#### Scenario: 公開鍵のローテーションに追随する
- **WHEN** ユーザーディレクトリの署名鍵がローテーションされ、キャッシュに無い鍵 ID のトークンが到達する
- **THEN** サーバは公開鍵を取得し直して検証を行う

#### Scenario: 存在しない鍵 ID を繰り返し提示される
- **WHEN** 公開鍵の集合に存在しない鍵 ID のトークンが繰り返し到達する
- **THEN** サーバは要求ごとに公開鍵の取得を繰り返さず、要求を拒否する

### Requirement: トークンの更新と失効
システムは、アクセストークンの有効期限が切れる前に更新しなければならない (SHALL)。更新できない場合は再認証を求めなければならない (SHALL)。

#### Scenario: トークンを更新する
- **WHEN** 保持しているアクセストークンが有効期限に近づく
- **THEN** システムはユーザーの操作を要さずにトークンを更新し、進行中の操作を中断しない

#### Scenario: 期限切れのトークンで要求する
- **WHEN** 有効期限を過ぎたトークンを伴う要求が API へ到達する
- **THEN** サーバは要求を拒否し、クライアントはトークンを更新して再試行する

#### Scenario: 更新に失敗する
- **WHEN** トークンの更新が失敗する
- **THEN** システムは保持しているトークンを破棄し、サインイン画面を表示して再認証を促す

#### Scenario: 未送信の入力を失わせない
- **WHEN** 入力フォームに未送信の内容がある状態で再認証が必要になる
- **THEN** システムは下書きを保持し、再認証後に復元する

### Requirement: ユーザーの登録
システムは、初回サインイン時に認証サービス上の識別子と対応するユーザーをアプリ側に登録しなければならない (SHALL)。アプリ側のユーザーレコードは、特定の認証サービスに依存しない形で識別子を保持しなければならない (SHALL)。

#### Scenario: 初回サインイン
- **WHEN** これまでにアプリを利用したことのないアカウントが初めてサインインする
- **THEN** システムは認証サービスの識別子に対応するユーザーを作成し、以後の投稿の作成者として関連付ける

#### Scenario: 2 回目以降のサインイン
- **WHEN** 既に登録済みのアカウントがサインインする
- **THEN** システムは既存のユーザーを再利用し、重複したユーザーを作成しない

### Requirement: トークン更新の主体
システムは、アクセストークンの更新を認証サービスとクライアントの間で完結させなければならない (SHALL)。API サーバがリフレッシュトークンを受け取ってはならない (MUST NOT)。

#### Scenario: 更新は API サーバを経由しない
- **WHEN** クライアントがアクセストークンを更新する
- **THEN** システムは認証サービスへ直接要求し、API サーバへリフレッシュトークンを送らない

#### Scenario: API サーバはアクセストークンのみを見る
- **WHEN** API 要求が到達する
- **THEN** サーバはアクセストークンのみを検証対象とし、リフレッシュトークンを保持も記録もしない

